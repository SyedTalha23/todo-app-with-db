<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    <div class="container">
        <h1><%= todaysDate %></h1>
        <h1>Todo List</h1>

        <div class="todo-list">
            <ul id="taskList">
                <% for(let i=0;i<todoList.length;i++){ %>
                    <li><div><input type="checkbox"><span><%= " "+ todoList[i] %></span></div> <form action="/removeItem" method="post"><input type="text" name="hiddenVal" type="hidden" value="<%= i %>" style="display:none;"><button type="submit">remove item</button></form></li>
                <% } %>
            </ul>
            <form action="/" method="post">
                <input type="text" id="taskInput" placeholder="Enter task..." name="taskInput" autofocus required>
                <button type="submit">Add</button>
            </form>
        <form action="/clearList" method="post"><button type="submit">clear list</button></form>
        </div>
    </div>

    <script src="js/script.js"></script>
</body>
</html>
